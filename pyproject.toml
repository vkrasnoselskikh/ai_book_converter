[project]
name = "ai-book-converter"
version = "0.1.0"
description = "Project for OCR - books to Epub conversion"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mistralai>=1.10.0",
    "python-dotenv>=1.2.1",
]
